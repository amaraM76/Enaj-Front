'use client'

import { LandingPagePreview } from '@/components/landing-page-preview'

// Simple preview page that bypasses the access gate
// This is just for design preview purposes - navigate to /preview to see the new design
export default function PreviewPage() {
  return <LandingPagePreview />
}
